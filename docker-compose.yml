version: "3"
services:

  nasa-rwanda:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nasa-rwanda
    volumes:
      - .:/app
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            device_ids: ['0']
            capabilities: [gpu]